# Loaded by `vite --mode static` and `vite build --mode static` (the GitHub Pages build).
# Base path of the project site; the deploy workflow overrides it from configure-pages.
VITE_BASE_PATH=/local-business-finder/
